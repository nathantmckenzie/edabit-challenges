//https://edabit.com/challenge/xP34EkkKFGQc6ZGwg
//very hard
//unsolved

function coinsDiv(arr) {
  let obj = {}
  let amount = arr.reduce((acc, cur) => acc + cur) / 3;
  for (let i = 0; i < arr.length; i++) {
     let currentValue = arr[i];
     let neededValue = 
  }
}

console.log(coinsDiv([1, 2, 3, 2, 2, 2, 3]));
