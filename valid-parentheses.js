//https://leetcode.com/problems/valid-parentheses/

var isValid = function (s) {
  let bracket = {
    "(": ")",
    "[": "]",
    "{": "}",
  };

  let heap = [];

  for (let char of s) {
    if (bracket[char]) {
      heap.
    }
  }
};

console.log(isValid("()[]{}"));
